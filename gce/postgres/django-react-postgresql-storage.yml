apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-react-postgresql-storage
  namespace: django-react
  annotations:
    volume.beta.kubernetes.io/storage-class: fast
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mb
