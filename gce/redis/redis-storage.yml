apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-react-redis-storage
  namespace: django-react
  annotations:
    volume.beta.kubernetes.io/storage-class: fast
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mb
